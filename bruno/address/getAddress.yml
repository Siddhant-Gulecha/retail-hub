info:
  name: getAddress
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/address/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
