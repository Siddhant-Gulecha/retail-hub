info:
  name: getAllAdresses
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/address/all"
  headers:
    - name: Authorization
      value: Bearer {{jwt_token}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
