info:
  name: updateAddress
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{base_url}}/address/3"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
          "addressLine": "456 Building"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
